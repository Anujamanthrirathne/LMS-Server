<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Reply Notification</title>
    <style>
        /* General email styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        /* Centering content and defining the maximum width */
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Header and Logo styling */
        .logo {
            display: block;
            margin: 0 auto 20px;
            width: 150px;
            height: auto;
        }

        h1 {
            font-size: 24px;
            color: #333;
            text-align: center;
            margin-bottom: 10px;
        }

        /* Paragraph styling */
        p {
            font-size: 16px;
            line-height: 1.6;
            color: #555;
            text-align: center;
            margin-bottom: 20px;
        }

        /* Button style */
        .cta-button {
            display: block;
            text-align: center;
            background-color: #007bff;
            color: #fff;
            padding: 12px 25px;
            font-size: 16px;
            text-decoration: none;
            border-radius: 5px;
            width: fit-content;
            margin: 0 auto;
            transition: background-color 0.3s ease;
            text-transform: uppercase;
            font-weight: bold;
        }

        .cta-button:hover {
            background-color: #0056b3;
        }

        /* Footer message styling */
        .footer {
            font-size: 14px;
            color: #777;
            text-align: center;
            margin-top: 20px;
        }

        /* Responsive Design: Mobile-friendly adjustments */
        @media only screen and (max-width: 600px) {
            .container {
                padding: 10px;
            }

            h1 {
                font-size: 20px;
            }

            p {
                font-size: 14px;
            }

            .cta-button {
                font-size: 14px;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <!-- Logo at the top -->
        <img src="https://logos.flamingtext.com/City-Logos/Chama-Logo.png" alt="Chama LMS Logo" class="logo">

        <!-- Greeting and message -->
        <h1>Hello <%= name %></h1>
        <p>A new reply has been added to your question in the video "<%= title %>".</p>
        <p>Please log in to our website to view the reply and continue the discussion.</p>

        <!-- Call to Action -->
        <a href="https://your-website.com/login" class="cta-button">Log in to View Reply</a>

        <!-- Footer -->
        <div class="footer">
            <p>Thank you for being a part of our community!</p>
        </div>
    </div>

</body>
</html>
